apiVersion: v1
kind: Service
metadata:
  name: backend-jaxrs-liquibase-graphql-api
  namespace: backend
spec:
  selector:
    app: backend-jaxrs-liquibase-graphql-api
  ports:
    - port: 8080
      targetPort: 8080
