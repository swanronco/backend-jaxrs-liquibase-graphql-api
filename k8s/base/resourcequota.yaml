apiVersion: v1
kind: ResourceQuota
metadata:
  name: backend-quota
  namespace: backend
spec:
  hard:
    requests.cpu: "1500m"
    requests.memory: "1000Mi"
    limits.cpu: "1700m"
    limits.memory: "1400Mi"
    pods: "10"
